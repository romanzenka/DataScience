A set of exercises using WebGL.

Should work simply by opening a given html file. For the examples that use textures, you would
want to run this through some sort of web server. 

IntelliJ users simply press the browser icon 
and get a web server running for them.

Others can use `python -m SimpleHTTPServer` after changing to the directory they want to see.
